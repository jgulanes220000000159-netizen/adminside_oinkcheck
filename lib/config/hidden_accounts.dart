class HiddenAccounts {
  /// Head Vet accounts to hide from Admin UI.
  /// Add the UID(s) here after creating the extra account.
  static const Set<String> hiddenUserIds = <String>{
    // 'PUT_HEAD_VET_UID_HERE',
  };
}

